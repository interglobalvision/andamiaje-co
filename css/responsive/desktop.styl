#logo-footer
  width: 200px

input
  padding: $margin-tiny 0 !important

#chevron
  width: 20px
  height: 20px

.full-height
  min-height: calc('100vh + ' + $margin-large)

#mockup
  max-width: 400px
